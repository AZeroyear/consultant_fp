<h1>登録画面</h1>

<% @user.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
<% end %>

<%= form_for(@user) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "アカウントを作成する" %>
<% end %>
